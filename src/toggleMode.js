export function toggleMode(){
   document.body.classList.toggle('dark')
}

// document.querySelector('.tog').addEventListener('click', function(event) {
//     // Check if the click occurred on the button with class 'theme-toggle' or within the <div class="tog">
//     if (event.target.matches('.theme-toggle') || event.currentTarget === event.target.closest('.tog')) {
//         // If the button or the <div class="tog"> itself was clicked
//         toggleMode();
//     }
// });

